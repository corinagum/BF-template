{
  "name": "bf-template",
  "version": "1.0.0",
  "description": "Bot Framework Template",
  "author": "corinagum",
  "license": "MIT",
  "main": "./lib/index.js",
  "scripts": {
    "build": "tsc --build",
    "eslint": "eslint src/**/*.ts --fix",
    "postinstall": "yarn build && node ./deploymentScripts/webConfigPrep.js",
    "start": "tsc --build && node ./lib/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "nodemon --watch ./src -e ts --exec \"yarn start\""
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/corinagum/BF-template"
  },
  "dependencies": {
    "@types/dotenv": "^8.2.0",
    "@types/restify": "^8.5.1",
    "botbuilder": "^4.13.0",
    "dotenv": "^8.2.0",
    "node-fetch": "^2.6.1",
    "nodemon": "^2.0.7",
    "replace": "^1.2.1",
    "restify": "^8.5.1"
  },
  "devDependencies": {}
}
