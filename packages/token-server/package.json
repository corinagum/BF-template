{
  "name": "bf-template-token-server",
  "version": "1.0.0",
  "description": "Token Server for BF-template",
  "main": "./lib/index.js",
  "private": true,
  "repository": "https://github.com/corinagum/BF-template",
  "author": "corinagum",
  "license": "MIT",
  "scripts": {
    "build": "tsc --build",
    "eslint": "eslint src/**/*.ts --fix",
    "start": "tsc --build && node ./lib/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "nodemon --watch ./src -e ts --exec \"npm start\""
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "math-random": "^2.0.1",
    "node-fetch": "^3.3.0",
    "restify": "^11.1.0"
  },
  "devDependencies": {
    "@types/node": "^18.8.2",
    "@types/restify": "^8.5.1"
  }
}
